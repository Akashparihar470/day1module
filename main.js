const allsum = require("./add") ;
const subtract = require("./subtract") ;
const divide = require("./divide") ;
const multiple = require("./multiply") ;


multiple.multiple();
divide.divide();
allsum.allsum();
subtract.subtract();